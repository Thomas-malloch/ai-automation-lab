# Samples

Local fixtures for the document-processing demo.

- `invoice_text_basic.txt` is the source text fixture.
- `invoice_basic.pdf` is a small text-based PDF fixture used by `make upload-sample`.

Additional generated or experimental sample documents are ignored by git.
