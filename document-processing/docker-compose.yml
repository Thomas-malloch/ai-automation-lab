services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: document_processing
      POSTGRES_USER: document_processing
      POSTGRES_PASSWORD: document_processing
    ports:
      - "5432:5432"
    volumes:
      - document_processing_postgres:/var/lib/postgresql/data

volumes:
  document_processing_postgres:

